{
  "name": "noclat.com",
  "version": "1.0.0",
  "description": "Nicolas Torres, full-stack web developer, designer and minimalist from Paris, France, crafting digital tools.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/noclat/noclat.com.git"
  },
  "author": "Nicolas Torres",
  "license": "MIT",
  "homepage": "https://noclat.com",
  "scripts": {
    "dev": "npm-run-all -p watch serve",
    "watch": "postcss -w styles/main.css -o static/main.css --no-map",
    "build": "postcss styles/main.css -o static/main.css --no-map",
    "serve": "serve . -l 3000",
    "deploy": "yarn clear && yarn build"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "cssnano": "^4.1.10",
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^6.1.3",
    "serve": "^11.2.0",
    "tailwindcss": "^1.1.4"
  }
}
